# Test automation for Contact List app using Cypress

This is a basic Cypress test automation repository for all functional tests for this website :
`https://thinking-tester-contact-list.herokuapp.com/`

The baseUrl is set in the Cypress config file : `cypress.config.js`

The test files are present in : `contact list app > cypress > e2e` folder